---
description: Run Unity tests via Lux batch mode
subtask: true
---

Run Unity tests (EditMode and/or PlayMode) via Lux batch mode.

Execute:

```bash
lux run-tests
```

Report test results. If tests fail, analyze the failures and suggest fixes.