---
description: Compile the Unity project via Lux batch mode
subtask: true
---

Compile the Unity project using Lux batch mode compilation.

Execute:

```bash
lux compile
```

Monitor compilation output. If compilation errors occur, read the error log and suggest fixes based on the project's spec and code structure.