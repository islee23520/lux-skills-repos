---
description: Trigger a Lux game build
subtask: true
---

Trigger a Lux game build for the current project.

Execute:

```bash
lux build --target webgl
```

Monitor the build output and report the result. If the build fails, analyze the errors and suggest fixes.